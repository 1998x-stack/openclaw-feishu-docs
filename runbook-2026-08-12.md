# OpenClaw + 飞书 运维沉淀（Session 2026-08-12）

> 目的：把一次深度配置/建群/审计/命名落地过程中**观察到的**环境事实、**验证过的流程**、**踩过的坑**、**已做的决策**沉淀成后续可续用的知识库。恢复现场看这篇 + `design-proposal.md`。

---

## 1. 环境快照（本机已核实）

| 项 | 值 |
|---|---|
| OS | Ubuntu / Linux 5.15，16 用户共享主机 |
| HOME | `/workspace/data/xieming` |
| OpenClaw | **2026.6.34** (5c38f99) — 2026-08-12 从 2026.6.8 (844f405) 升级 |
| feishu 插件 | `@openclaw/feishu 2026.6.34`（`npm/projects/openclaw-feishu-dc69f44688__openclaw-generation__…e97037f4d0`） |
| Node | nvm v24.17.0 |
| Gateway | systemd user service，`127.0.0.1:18789`（loopback），token 认证 |
| 今日日志 | `/tmp/openclaw/openclaw-2026-08-12.log` |
| 旧日志 | `~/.openclaw/logs` |

**4 个 Feishu bot / agent 映射**（含群、工作区与人格）：

| bot | agent | 应用 appId | 群 chat_id | 群名(落地) | 工作区(v3 改名后) | 人格(IDENTITY.md) |
|---|---|---|---|---|---|---|
| default | main | `cli_aab920536efa1cc8` | `oc_35c62..a76d` | **OpenClaw·阿岩** | `workspace-main` | 阿岩 ⛰️ 总指挥 |
| newbot | newbot | `cli_aaff5a3..dccd` | `oc_83ff..32c3` | **OpenClaw·小七** | `workspace-ops` | 小七 🔧 运维 |
| bot2 | bot2 | `cli_aafc1e4..ce9` | `oc_ebe..20d5` | **OpenClaw·老六** | `workspace-lab` | 老六 🐍 代码 |
| bot3 | bot3 | `cli_aafc1f6..cb0` | `oc_ffe9..4031` | **OpenClaw·阿叁** | `workspace-lib` | 阿叁 📚 资料 |

安全归属：owner 均为用户本人（各 `ou_…`），config 里 4 账号皆 `dmPolicy+groupPolicy=allowlist`、`groupSessionScope=group_topic`、`replyInThread=enabled`。

---

## 2. 关键观察（已验证的机制/事实）

### 2.1 插件 schema vs 运行时 ≠ 同一张表
- `openclaw.plugin.json` 的 `configSchema` **只列出** `enabled/defaultAccount/appId/appSecret/…/streaming/blockStreaming/replyInThread/typingIndicator/accounts`。
- 但 **dist 运行时** 额外读取：`dmPolicy`、`allowFrom`、`groupPolicy`、`groupAllowFrom`、`groupSenderAllowFrom`、`groupSessionScope`。这些**不在 schema 却在运行时有意义**（`additionalProperties:true` 宽容了）。
- **验证方法**：`grep -oE '"group[A-Z][A-Za-z]+|dm[A-Z][A-Za-z]+' dist/channel-Dwyxyxx.js dist/monitor.account-*.js`。
- 教训：改 OpenClaw feishu 配置，**以 runtime 实际读取为准**，别只看 schema/文档。

### 2.2 群 session scope 枚举（版本 2026.6.8）
运行时 `GroupSessionScopeSchema=z.enum([...])` 支持：`group / group_sender / group_topic / group_topic_sender`。`group_topic`（+ `replyInThread=enabled`）= 一话题一个 Agent session，最强隔离。

### 2.3 WebSocket 模式
当前全部 bot 用 `connectionMode: websocket`（不需公网 webhook）。日志能看到：`feishu[bot]: starting WebSocket connection` → `WebSocket client started`。长连接提示是 SDK info 级，非错误。

### 2.4 `openclaw doctor` 是有用但非万能的
- 能查 `Errors:0`、Plugins errors。
- 但**提示**：`openclaw.json 包含明文 secret`（架构性事实，需自己采纳 secretRef 才消）。
- 还提示：`systemd 服务 PATH/Node 来自 version manager`（可改进项）。

### 2.5 Feishu「工具调用可见」机制（2026-08-12 三审，直读 dist 确认）
- **呈现方式**：feishu 无 opencode 式工具块；工具进度 = **流式交互卡里的 status 行**（`🛠️ exec: …` 系）。链路：core `onToolProgress` → `formatChannelProgressDraftLineForEntry` → feishu monitor `updateStreamingStatusLine` → 卡片文本拼接（streamText + statusLine）。
- **生效条件**（当前配置已全开）：`channels.feishu.streaming !== false`（需 `true`）且 `renderMode !== "raw"`（auto/card）且 `blockStreaming === true`（块尽早冲卡）；数据丰度由 `agents.defaults.toolProgressDetail`（`raw`/`explain`）控制。
- **⚠️ schema 陷阱（runbook §2.1 又中一例）**：本版（2026.6.8）feishu 插件 schema 把 `channels.feishu.streaming` **钉死为 boolean**；核心代码支持对象形态（`streaming:{mode,block,preview}`），但**插件配置校验会拒收**（`invalid config: must be boolean`）→ 别信文档的 discord 对象写法，feishu 用 `true`。
- **已知限制**：① status 行偏"当前工具"，不是持久化的工具清单；② **纯工具无文本的运行不产卡** → 触发 `no-visible-reply fallback` 兜底文字（日志 ERROR 级，非故障）；③ 若要强制所有消息走卡片可试 `renderMode:"card"`，默认 `auto` 已对 final 文本用流式卡，一般不必要。
- **⚠️ 真相修正（2026-08-12 实测 + 源码铁证）**：以上"配置已全开即可见工具行"的结论**作废**。群聊(room_event)在引擎层走 `sourceReplyDeliveryMode = "message_tool_only"`（dist `source-reply-delivery-mode-wBpPJDPa.js` 硬规则，先于一切配置）→ **群内最终回复默认私有，必须由模型显式调用 `message(action=send)` 工具才可见**；且该模式下工具进度行被同步抑制（`agent-runner.runtime`：`message_tool_only && !shouldEmitToolResultProgress() → return`）。实测（转录为证）：iagent/standard 在群内干满 12 次工具调用并写出完整回复，但**从未调用 message 工具 → 卡开即空关，`queuedFinal=false, replies=0`，兜底因 mode 豁免不触发**。`messages.groupChat.visibleReplies`（dispatch-B0 支持）被 room_event 硬规则挡在身前，改它无效入站群消息。CLI `openclaw agent --deliver` 之所以成功，是因走显式投递路径、非 room_event。
- **✅ 唯一能验证/恢复群可见性的候选**：(a) 模型若肯调 `message(send)` → 回复可见（工具行在 message_tool_only 下仍被抑制）；(b) DM（非 room_event）路径预计默认自动可见——待实测确认。
- **✅ 终局结论（2026-08-12 四轮实测闭环）**：① **文本块流式（DM）✅ 可用**——根因却是配置作用域坑：feishu 插件读取的 `blockStreaming`/`renderMode` 均为**账户级**（`account.config.*`），顶层 `channels.feishu.*` 只对默认账号生效；`accounts.{newbot,bot2,bot3}` 补 `blockStreaming:true` + `renderMode:"card"` 后，DM 长任务出现逐段滚出文字+最终总结。② ~~工具行不可得~~（已被下一条翻案）。③ 群内 `message_tool_only` 硬规则不变；模型可被引导调用 `message(action=send)`（07:23 实证 198 字 committed，但当时块流未开未送达——修复后群内 message 路径待重验）。④ 账户级 vs 顶层配置作用域 = 全天最大坑，四连击（streaming schema、blockStreaming、renderMode、消息限制）。
- **⭐ 工具行翻案（2026-08-12 升级 6.34 后 DM 实测）**：②「工具行不可用=能力边界」**作废**。升级 2026.6.34 后链路已接通：核心 `forwardFollowupProgressEvent`（agent-runner.runtime）发射 **`onToolStart`**（注意 6.34 已换名，6.8 的 `onToolProgress` 废弃——按旧名 grep 得 0 是假象）→ 插件 `formatChannelProgressDraftLineForEntry` → `updateStreamingStatusLine` → 卡片 status 行。**真正的锁是 `agents.defaults.verboseDefault`（合法值 off|on|full）**：`resolvedVerboseLevel` 未配置→`"off"`→`shouldEmitVerboseProgress=false`→`emitChannelProgress=false`→进度事件被闸门拦。**已配 `verboseDefault:"on"` + 既有 `toolProgressDetail:"raw"` → DM 实测卡上出现 `🛠️ uname -a` ✅**。显示在正文**末尾**是设计使然（`buildCombinedStreamText` 拼接 thinking+`---`+streamText+statusLine，statusLine 恒在尾部）。`queuedFinal=false, replies=0` 正常：最终文本走卡片 finalize。⚠️ 群聊(room_event)下 `message_tool_only` 对工具行/最终回复的抑制逻辑仍在（`message_tool_only && !shouldEmitToolResultProgress() → return`），**群内工具行待单独验证**。
- **实测补充（2026-08-12 三审当日）**：① 06:47 用户在老六群发"你好"→ 日志实证全链路（dispatch → `Started streaming: cardId=…` → `Closed streaming`）——改名后入站 e2e ✅；② `openclaw agent --channel feishu --deliver --agent bot2 --reply-account bot2 --reply-to <oc_…>` 可自驱投递带工具任务的回复进群（回执 exit 0，但**该投递路径不回写主网关日志**，也无流式卡痕迹）；③ 想用 API 读回群消息作真值需在应用补 `im:message.group_msg` 读权限（当前 4 应用均无，API 返回 code 230027）；④ 工具行视觉确认仍待用户在群内跑一次多工具任务。
### 2.6 群的 P2P vs 群
- 日志里 `received message … in oc_… (p2p)` = DM / 单聊会话 ID，不是群。群内是 `(group)`。
- ~~早期 phase 只见 p2p —— 群消息端到端仍未在日志验证过~~ → **【修正 2026-08-12 二轮审查】**：翻当日日志确证群端到端**早已验证**——03:25:49 (default=阿岩群)、03:27:20 (newbot)、03:27:27/49 (bot2)、03:27:37/55 (bot3) 起连续收到 4 群 `(group)` 用户消息，至 04:25 共 10 条。当时只看了早期 p2p 切片即下结论，属"落盘前未复核最新日志"的教训（见 §8.3 已转完成）。

---

## 3. 建立/改名飞书群流程（已验证可复现）

### 3.1 鉴权
```python
POST /open-apis/auth/v3/tenant_access_token/internal
{app_id, app_secret} → tenant_access_token（勿外泄）
```

### 3.2 建群
```bash
POST /open-apis/im/v1/chats?user_id_type=open_id
{name, user_id_list:[owner_ou], owner_id}
```
- **成功**返回 `chat_id`（`oc_…`），`chat_mode: group`。
- 建群**只需** `im:chat`，但把 bot/成员加进群**需要** `im:chat.members:write_only`（99991672 权限错误在页面 `?q=im:chat.members:write_only` 申请）。

### 3.3 改群名（重点坑）
- ❌ `PATCH` → **404**（飞书此端点不接受 PATCH）
- ✅ **`PUT`** → success（`/open-apis/im/v1/chats/{chat_id}`，body `{name}`）
- 已验证所有 4 群 rename 成功。

### 3.4 发消息进群（验证收到）
```bash
POST /open-apis/im/v1/messages?receive_id_type=chat_id
{receive_id: chat_id, msg_type:"text", content:{"text":"..."}}
```

---

## 4. 安全审计流程（subagent 全并行只读）

派 4 个并行只读 agent，互不干扰：
1. 安全与密钥（找 exposed secret、perms、git）
2. Feishu 路由 review（allowlist / binding / sessionScope）
3. 人格/工作区/记忆（SOUL/IDENTITY/AGENTS/记忆、改名影响）
4. 运行时健康与日志（systemd、300s queue evict、websocket）

**统一教训**：观察矛盾时以直读文件为准——agent 报告是二手，需用 `ls 权限/find modulo/git status` 复核认定。

---

## 5. 坑与教训（已踩、已验证）

### 5.1 systemd 加固会炸 gateway
注入 `NoNewPrivileges/UMask/RestrictSUIDSGID/ProtectClock/KeyringMode/ProtectHostname` 后：
- gateway `state activating, last exit 218, auto-restart`（无法启动）。
- **回滚到备份 unit 立即恢复**（`systemctl --user daemon-reload && restart`，4 bot reconnect）。
- 结论：当前 OpenClaw 0.320 与**进程限制类** hardening 选项冲突（首轮是 NoNewPrivileges/UMask/RestrictSUIDSGID/ProtectClock/KeyringMode/ProtectHostname 整组注入，无法归因到单条）。OpenClaw 须 HOME 写 .openclaw（state/mem/logs/…）—— 若强制 `ProtectHome` 更会破坏持久化。**勿直接加 hardening，除非确认选项兼容。**
- 【修正 2026-08-12 二轮审查】`UMask=0077` **单独验证通过**（§5.2 已落地持久化）：它只改新建文件/目录模式，不限制进程能力，与 5.1 的进程限制类选项不是一类；但**禁止与之并列注入任何 Protect*/NoNewPrivileges 类选项**。

### 5.2 权限收敛（重要成功 · 二轮修正）
将**共享主机**上 664/775 敏感文件收敛：
| 对象 | 前 | 后 |
|---|---|---|
| 4 个 `agents/*/sessions/` dir | 775 | **700**（**第一轮失效 → 二轮已持久化**，见下） |
| 各 `.jsonl.deleted.*` 残留（186 个） | 664 | **600** |
| `~/.bashrc`（含 iagent key） | 644 | **600** |
| `~/.config/opencode/opencode.jsonc`(.bak) | 664 | **600** |
| `~/.claude/transcripts/` | 775 | **700** |
| `.openclaw/workspace*` dirs | 775(受 700 父目录保护) | 另议 |

> **【修正 2026-08-12 二轮审查】sessions 目录 775→700 第一轮失效**：首轮 chmod 700 后，gateway 重启（04:36）即把 4 个 sessions 目录以 **0777 & ~umask 重建为 775**（实测 ctime=04:36:01 与 gateway 启动 04:35:58 吻合）。根因：unit 默认 `UMask=0002`，凡目录缺失即重建 775。**持久化方案（已落地并重启验证）**：unit 加 `UMask=0077`（备份 `~/.config/systemd/user/openclaw-gateway.service.bak.20260812`）→ 重启后 sessions 目录**由网关直接创建为 700**、新会话文件 600，4 bot ws 重连正常。教训+1：**权限收敛后必须重启验证一次，否则是"记录型修复"**。
> `.openclaw/workspace*` 现有 775 目录不受 umask 影响（umask 只管新建）——仍在 700 父目录保护下，维持"另议"。

### 5.3 key 撤布范围（未轮换——用户明确决定不轮换）
- iagent API key `igw_1d63…` 存在于：`openclaw.json/models.json(.bak)/.corpus`、`.bashrc`、`opencode.jsonc(.bak)`、`.iauto_api_key`、**所有 claude transcripts**、以及 `iauto-codes/iagent/benchmarks/token-speed-benchmark.sh`。
- 最后这个文件：**曾已被 git 跟踪**（`git log --diff-filter=A` 确认 45b24f4 "Initial commit" 即入库，repo `iauto-codes/iagent`，无 remote）。
- **【修正 2026-08-12 二轮审查】** 首轮结论「未被 git 跟踪」是**误判**：当时只查了 `.gitignore`/remote 就定性，而 **gitignore 对已跟踪文件无效**、`git check-ignore` 也查不出已跟踪文件。二次核验用 `git ls-files benchmarks/` + `git log` 证实文件**确实在索引与历史里**。处置：`git rm --cached benchmarks/token-speed-benchmark.sh`（移出索引，本地文件保留 4871B）→ `.gitignore` 第 6 行 `**/token-speed-benchmark.sh` 现已真正生效（check-ignore exit 0）。**注意**：git 历史仍含该 key，无 remote 所以暂不构成远程泄露；**将来若加 remote/共享仓库，须先处理历史（filter-repo）**。变更尚未 commit（等用户拍板）。
- 用户决定：**不轮换 key**。故保留（只是收敛权限 + 移出索引，降低同机可读与误提交风险）。

### 5.5 模型限制错配 = 长时间卡死 + ⚠️failed 摘要的根因（2026-08-12 四审修复）
- **现象**：运行时间过长（30min lane `durationMs=1800536` 级超时）→ 群里收到 `⚠️ 🛠️ print text → list git … → run git merge-base (in <cwd>) failed` 式**工具链失败摘要**（`tool-display-common`：`→` 连接单次 exec 的多段命令 stage；正常进度不可见、失败摘要可见——群 message_tool_only 模式的又一体现）。
- **根因**：`models.providers.iagent.models[*]` 配置 `contextWindow: 524288 / maxTokens: 65536`，而 iagent 供应商实际上限 **131072**（日志实证：`the model's context length is only 131072 tokens…You passed 65537 input and requested 65536 output`）→ 会话一长即超限 → 自动压缩失败（`auto-compaction failed…400`）→ 卡至超时 → 投失败摘要。
- **修复（已落地）**：3 个模型统一 `contextWindow: 131072`、`maxTokens: 16384`（备份 `openclaw.json.bak.20260812-models`）。doctor/重启验证通过。预期：输入不会超真实上限→compaction 不再 400→运行时长回到分钟级→失败摘要减少。**复查**：若仍有超时，再查 `agents.defaults.timeoutSeconds`（现 1800）与 `reasoning_effort:max` 的开销。
### 5.4 hallucinated/git 误报
- 审计 agent 报告 "iagent key tracked by git" —— 首轮"实测非 git/无 remote，二次核实给正确"是**误判**。
- **【修正 2026-08-12 二轮审查】** 用 `git ls-files` + `git log --diff-filter=A` 复核：**agent 原报告正确**——文件确实在 45b24f4 已入库（详见 §5.3）。首轮犯了 "check-ignore/status 看不到已跟踪文件" 的方法错误。教训+1：**判断"是否被 git 跟踪"唯一可靠命令是 `git ls-files`**。

---

## 6. 已做决策（本会话定）

1. **不轮换** iagent key（用户拍板）→ 收敛权限 + 移出 git 索引即可。
2. **不替 `iagent/standard` 加 tool deny**（用户拍板）→ 保留 web/browser/shell 能力；把现状记录为"默认模型全工具"。
3. **命名体系**：阿岩(main)/小七(newbot/运维)/老六(bot2/代码)/阿叁(bot3/资料)；群名 OpenClaw·X（已改）；account 显示名（openclaw.json `channels.feishu.accounts.*.name` 直读核实）已改为 **小七（运维工）/老六（代码实验室）/阿叁（资料库）**。（原稿此处有「冷水」碎片与乱码人名，二轮审查时按 config 实况修正）
4. **工作区改名**：设计稿建议 workspace-main/ops/lab/lib vs 罗马音；**已于 2026-08-12 二轮执行 A 方案并落地**（`workspace-main/ops/lab/lib`）。⚠️ 关键机制（dist `resolveAgentWorkspaceDir` 直读）：main 无 workspace 行时走 `resolveDefaultAgentWorkspaceDir` = `$HOME/.openclaw/workspace`；给 main 新增显式 `workspace` 行后改走配置路径。故落地时 config 共改 4 行（3 行路径改 + main 新增 1 行），agentDir/agentId/bindings 均未动。
5. **sessions 权限持久化**（二轮审查落地）：systemd unit 加 `UMask=0077`（备份 `.bak.20260812`），重启实测 sessions 目录 700 / 文件 600，4 bot 正常。
6. **key 脚本移出 git 索引**（二轮审查落地 + 已 commit `f3cc549`）：新文件 `benchmarks/token-speed-benchmark.sh` 本地保留，索引与历史之外。
7. **人格文件已写入**（v3 落地）：4 工作区各 SOUL/IDENTITY/USER/AGENTS 更新完毕（IDENTITY 队名 阿岩/小七/老六/阿叁 与群名/账号名一致；`openclaw agents` 已验证 runtime 读到新人格+新工作区）。

---

## 7. 当前状态追踪（2026-08-12 落盘时）

**已完成**
- Feishu 安全白名单（4 账号 allowlist + group allow + sender 白名单 + group_topic scope）。
- 4 个群创建 + 改名 OpenClaw·阿岩/小七/老六/阿叁（PUT，已验证 4 群 rename 成功）。
- openclaw.json account 显示名：小七（运维工）/老六（代码实验室）/阿叁（资料库）（config 直读复核；原稿"小鲅/老杨"系乱码，已修正）。
- 权限收敛（上表）/ gitignore / systemd hardening 回滚 + **UMask=0077 持久化**（gateway 健康、4 bot ws 正常）。
- ✅ 群消息 end-to-end 已验证：03:25–04:25 4 群 `(group)` 接收事件共 10 条（§2.5）。
- ✅ sessions 权限二轮收敛（UMask 持久化，重启实测 700 保持）。
- ✅ key 脚本出 git 索引（commit `f3cc549`）。
- ✅ **工作区改名落地**（2026-08-12 晚）：`workspace/{newbot,bot2,bot3}` → `workspace-{main,ops,lab,lib}`；openclaw.json 4 行（main 新增显式行）；备份 `openclaw.json.bak.20260812-rename`；doctor 通过、重启 4 ws 正常、`openclaw agents` 解析新路径。
- ✅ **人格文件写入**：4 工作区 SOUL/IDENTITY/USER/AGENTS 全部更新（阿岩/小七/老六/阿叁），`openclaw agents` 显示 runtime 读取新人格成功。
- ✅ **人格文件二次审校**（4 子代理并行，2026-08-12）：修正标点全角化、措辞、旧名残留（ops 清除 newbot 提法；lab/lib 半角冒号→全角等）；金标准 4 行小队表×4 工作区逐字复核通过；错别字扫描 0。

**进行中/未决**
- ⏸ 群消息在改名后的新群再 end-to-end 自验一次（可选，非必需）。
- ⏸ 若将来给 `iauto-codes/iagent` 加 remote：先 filter-repo 清 key 历史。

---

## 8. 待办 / 下一步（建议顺序）

1. ✅ (done) 人格文件 4 份 SOUL/IDENTITY/USER/AGENTS 已写入（2026-08-12 v3；IDENTITY 旧名 老七/007 已替换 小七/阿叁，main 空模板已填阿岩）。
2. ✅ (done) 工作区改名 A 方案已落地：main 默认解析机制已查明（§6.4），mv+config 4 行+doctor+重启+`openclaw agents` 校验通过。
3. ✅ (done) 群消息 end-to-end：已于 03:25–04:25 在 4 群日志验证 `(group)` 接收（无需再做，除非换新群）。
4. (低优先) 其余加固单条（`RestrictNoNewPrivileges` 等）如需再上：一条一条单独验证副作用；`UMask=0077` 已达标落地。
5. (常规) 每次 gateway 重启后：`stat -c '%a' ~/.openclaw/agents/*/sessions` 应均为 700（UMask 已使其自愈，复核仅 5 秒）。
6. ✅ (done) key 脚本出索引已 commit（f3cc549）；若将来加 remote，先 filter-repo 清历史。
7. (可选) 改名后的群（OpenClaw·阿岩/小七/老六/阿叁）内 @ bot 端一次验证，确认无回归。

---

## 9. 升级到 2026.6.34 extended-stable（2026-08-12 执行并验证）

**背景**：用户选择走 extended-stable 路线（6.x 线内最稳），目的是获得 7 月 feishu 流式卡片修复（防重复 #103915、flush 计时器 #99301、卡片 API 30s 超时 #102948、blockStreaming 独立消息 #94250），而非追求工具行（上节已证=能力边界）。

**执行步骤（可复现）**：
1. 备份：`~/.openclaw/backups/upgrade-2026.6.34-<ts>/` 含 openclaw.json、systemd unit、feishu 插件 tgz（config sha256 `5406f228…`）。
2. 核心升级：`npm install -g openclaw@extended-stable`（dist-tag 直接命中 2026.6.34；不要用 `latest`=2026.7.1）。
3. 插件升级：`openclaw plugins update --all`（CLI 自动选 **与运行时不兼容的 latest 降级为 newest compatible 2026.6.34**；新 plugin 装入 `__openclaw-generation__…` 目录，旧 6.8 目录保留）。
4. `openclaw doctor --fix`：自动迁移账户策略结构（`dmPolicy/allowFrom/groupPolicy/groupAllowFrom` 从 feishu 顶层**下移到 `accounts.default`**）、清 stale `plugins.entries.deepseek`、重启服务。⚠️ 注意：**顶级 `blockStreaming=true` + 账户级 `blockStreaming:true/renderMode:card` 原地保留**——6.34 schema 仍用 `BlockStreamingSchema`（扁平键未被拒收，librarian 情报"扁平处处拒收"仅对 core 通用 `streaming` 概念成立，feishu 插件是独立 schema）。
5. systemd unit 版本标记手动同步（Description/`OPENCLAW_SERVICE_VERSION` 改 2026.6.34，`daemon-reload`）——否则 `gateway status --deep` 报 "service installed by 2026.6.8, current is 2026.6.34"。

### 升级后验证（全部通过）
- `openclaw --version` = 2026.6.34 (5c38f99)；`gateway status --deep`：CLI/Gateway 双 2026.6.34、Runtime running、Connectivity probe ok。
- 4 账号 WebSocket 全部 `starting → WebSocket client started`（open_id 正常解析，event-dispatch ready）。
- `openclaw agent --agent main --channel feishu -m "…"` 端到端：winner iagent/standard，`"text": "正常"`，stopReason=stop（无 fallback）。
- `openclaw config validate`：Config valid。
- 插件代码核查：6.34 dist `toolProgress/onToolProgress` 命中 **0** 曾误判"工具行不可用"——实为 6.34 换名 `onToolStart`（详见 §2.5 工具行翻案）；配 `verboseDefault:on` 后 DM 实测 ✅ 工具行可见。

### 升级残留/已知项
- logs `config-health.json` 遗留冲突（doctor 每次提示 "Left legacy config health state…"）：无害，等上游迁移或手动清理。
- nvm PATH/Node 提示仍在（架构性，非本次引入）。
- DM 流式行为升级后已实测 ✅（工具行 `🛠️ uname -a` 出现在卡片；`verboseDefault:"on"`）；群内 message_tool_only 硬规则不变。

---

> 附加：`/workspace/data/xieming/openclaw-design/design-proposal.md` 存命名方案细节。
> 本文件偏向流程与事实；两者互补。
>
> 🔄 本文件于 2026-08-12 二次审查（全面自查）后修订：修正 §2.5/§5.2/§5.3/§5.4/§6/§7/§8 的过期或错误结论，落地 UMask=0077 与 git 清理；并随后续三轮执行更新至最新（工作区改名、人格文件、git commit f3cc549）。原稿与修订均以实测为准。
> 🔄 2026-08-12 追加 §9：升级 2026.6.34 extended-stable（含升级步骤、验证、残留项）。